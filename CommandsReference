-- Linux

date

which nano|vi
cat, touch, >> , &&, pwd cd ls -la, scp, cp, mv

su -u, sudo
sudo -u springboot

-- apt
apt edit-sources
apt search openssh-client
apt update && apt full-upgrade
apt install 
apt purge | remove
apt list
apt list-installed | grep ^nano
apt show nano
apt install snapd



-- ssh
apt install openssh-client
ssh-keygen -t rsa -b 4096


-- webservice
python3 -m http.server 7600
npx http-server -p 7800
php -S localhost:8080
/etc/apache2/ports.conf. systemctl start apache2


-- Docker

docker pull centos
docker build -t domainlister .
docker rmi image

docker run -dt --name jenkins -p 8080:8080 -p 5000:5000
        -v jenkins_home/var/lib:lib/jenkins_home jenkins:latest

docker start|stop|rm containername

docker ps
docker stats

docker exec -it -u 0 centos bash

docker logs jenkins



--ccna
enable
show mac-address-table
show ip route 
